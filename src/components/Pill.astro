<div class="pill"><span class="pill-prefix" aria-hidden="true">#</span><slot /></div>

<style>
  .pill {
    display: inline-flex;
    align-items: center;
    padding: 0.3rem 0.75rem;
    gap: 0.2rem;
    color: var(--accent-text-over);
    border: 1px solid var(--accent-regular);
    background-color: var(--accent-regular);
    border-radius: 999rem;
    font-family: var(--font-mono);
    font-size: var(--text-sm);
    line-height: 1.35;
    white-space: nowrap;
  }

  .pill-prefix {
    opacity: 0.7;
  }
</style>
